<?xml version="1.0" encoding="ISO-8859-1"?>
<odoo>

    <record id="ipam_label_tree" model="ir.ui.view">
        <field name="name">ipam.label.tree</field>
        <field name="model">ipam.label</field>
        <field name="arch" type="xml">
            <tree string="Label">
                <field name="name"/>
                <field name="color"/>
            </tree>
        </field>
    </record>

    <record id="ipam_label_form" model="ir.ui.view">
        <field name="name">ipam.label.form</field>
        <field name="model">ipam.label</field>
        <field name="arch" type="xml">
            <form string="Label">
                <header>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="toggle_active" type="object" class="oe_stat_button" icon="fa-archive">
                            <field name="active" widget="boolean_button" options="{'terminology': 'archive'}"/>
                        </button>
                    </div>
                    <group>
                        <field name="name"/>
                        <field name="color"/>
                    </group>
                    <field name="network_ids">
                        <tree string="Networks">
                            <field name="network_address"/>
                            <field name="netmask"/>
                            <field name="cidr"/>
                            <field name="gateway"/>
                            <field name="network_type"/>
                            <field name="vlan_id"/>
                        </tree>
                    </field>
                </sheet>
            </form>
        </field>
    </record>

    <record id="ipam_label_action" model="ir.actions.act_window">
        <field name="name">Labels</field>
        <field name="res_model">ipam.label</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="o_view_nocontext"></p>
        </field>
    </record>
</odoo>